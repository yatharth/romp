# pretty-romp PR screenshots

Before/after screenshots for romp-on/romp#730, rendered headlessly from the SYNTHETIC
fixtures in `tools/ui-verify/fixtures/` (the notes-api demo domain — no real session data).
Disposable branch: safe to delete once the PR is closed.
